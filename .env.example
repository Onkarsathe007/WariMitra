# Infrastructure (used by docker-compose)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password

# Service-specific env vars are in each service's .env.example
# Copy them: cp core-api/.env.example core-api/.env
